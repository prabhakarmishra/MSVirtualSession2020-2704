spring:
  cloud:
    config:
      uri: ${discountms.configserverurl:http://192.168.99.100:8888}
      fail-fast: false
  application:
    name: discountms
  profiles:    active: dev

server:  port: 0